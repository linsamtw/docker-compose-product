version: '3.3'
services:
  api:
    image: test:dev
    ports:
      - target: 5555
        published: 5555
        mode: host
    hostname: api
    restart: always
    environment:
      - TZ=Asia/Taipei
    networks:
        - dev

networks:
  dev:
